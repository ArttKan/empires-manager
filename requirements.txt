fastapi==0.115.0
uvicorn[standard]==0.30.6

# Needed by fastapi.testclient, which deploy.sh's test gate uses.
# The version was taken from the server's venv (Python 3.10), which is the
# environment that matters.
httpx==0.28.1
